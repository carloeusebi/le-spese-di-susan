import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.starter',
    appName: 'Le spese di Susan',
    webDir: 'dist',
    server: {
        androidScheme: 'https',
    },
};

export default config;
